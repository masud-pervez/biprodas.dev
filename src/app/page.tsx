import { ModeSelect } from '@/components/shared/mode-select';
import { Button } from '@/components/ui/button';
import React from 'react';

const Home = () => {
  return (
    <div>
      Biprodas R.<Button className="mx-3">Click me</Button>
      <ModeSelect />
    </div>
  );
};

export default Home;
